global:
  resolve_timeout: 5m

route:
  group_by: [alertname, severity]
  group_wait: 10s
  group_interval: 5m
  repeat_interval: 1h
  receiver: log-receiver

receivers:
  - name: log-receiver
    webhook_configs:
      - url: http://localhost:9093/api/v1/alerts
        send_resolved: true
